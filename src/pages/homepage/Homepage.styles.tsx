import styled from 'styled-components';

const HomepageWrapper = styled.div`
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    gap: 6px;
`;

export {
    HomepageWrapper,
};